import React from 'react';

/**
 * FloatingTimerWidget - Disabled per user request.
 * Timers only run inline within their respective quest cards.
 */
export const FloatingTimerWidget = () => {
  return null;
};
